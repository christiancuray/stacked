{
    "name": "stacked",
    "private": true,
    "scripts": {
        "dev:frontend": "npm --prefix stacked/frontend run dev",
        "dev:backend": "cd stacked/backend && mvn spring-boot:run",
        "dev": "npm-run-all -p dev:backend dev:frontend"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
